

import 'package:flutter/material.dart';

PreferredSizeWidget custumsAppBar() {
return AppBar(
        backgroundColor: Colors.white,
        title: const Text(
          'BMI-Rechner',
          style: TextStyle(color: Colors.black,),
          ),
          );
 }          